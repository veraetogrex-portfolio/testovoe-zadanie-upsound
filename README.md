# Telegram-бот для парсинга треков Яндекс.Музыки

Бот для получения информации о треках с Яндекс.Музыки через Telegram.

## Возможности

- **Получение информации о треке** - название, артист, длительность, альбом
- **Поддержка множества доменов** - .ru, .com, .by, .kz, .ua
- **4-этапная валидация**:
  - Этап 1: Валидация типа данных
  - Этап 2: Валидация формата URL (детектирование альбомов, артистов, плейлистов)
  - Этап 3: HTTP-запрос с retry-политикой (автоматические повторные попытки)
  - Этап 4: Парсинг и валидация JSON-LD данных
- **Умная обработка ошибок** - 30+ типов исключений с понятными сообщениями
- **Retry-механизм** - автоматические повторы при временных сбоях (502, 503, 504, timeout)
- **Graceful degradation** - fallback-значения для необязательных полей
- **Детальное логирование** - для отладки и мониторинга
- **Асинхронная архитектура** - высокая производительность

📖 **Подробнее:** [Документация по обработке ошибок](ERROR_HANDLING.md)

## Требования

- Python 3.10+
- Зависимости из [requirements.txt](requirements.txt)

## Установка

### 1. Клонируйте репозиторий

```bash
cd yandex_music_bot
```

### 2. Создайте виртуальное окружение

```bash
python -m venv venv
venv\Scripts\activate  # Windows
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Настройте переменные окружения

Создайте файл `.env` на основе `.env.example`:

```bash
copy .env.example .env
```

Откройте `.env` и добавьте токен бота:

```env
BOT_TOKEN=ваш_токен_от_BotFather
LOG_LEVEL=INFO
```

**Как получить токен:**
1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен в `.env`

## Запуск

```bash
python run.py
```

После запуска бот будет работать в режиме polling и отвечать на сообщения пользователей.

Лог бота будет выводиться в консоль. Для остановки нажмите `Ctrl+C`.

## Использование

### Команды бота

- `/start` - Приветственное сообщение и инструкция
- `/help` - Подробная справка по использованию

### Как получить информацию о треке

1. Запустите бота командой `/start`
2. Зайдите на [music.yandex.ru](https://music.yandex.ru)
3. Найдите нужный трек
4. Скопируйте ссылку на трек
5. Отправьте ссылку боту в чат

### Поддерживаемые форматы ссылок

```
https://music.yandex.ru/album/{album_id}/track/{track_id}
https://music.yandex.ru/album/{album_id}/track/{track_id}?from=serp
```

### Пример

**Вы отправляете:**
```
https://music.yandex.ru/album/3192570/track/26464125
```

**Бот возвращает:**
```
🎵 Название: Группа крови
👤 Артист: Кино
⏱️ Длительность: 4:47
💿 Альбом: Группа крови
```

## Структура проекта

```
yandex_music_bot/
├── src/
│   ├── __init__.py
│   ├── bot.py                    # Точка входа приложения
│   ├── config.py                 # Конфигурация
│   ├── handlers/
│   │   ├── __init__.py
│   │   └── common.py             # Обработчики команд и сообщений
│   ├── services/
│   │   ├── __init__.py
│   │   └── yandex_parser.py      # Парсер Яндекс.Музыки
│   ├── models/
│   │   ├── __init__.py
│   │   └── track.py              # Модели данных (TrackInfo)
│   └── utils/
│       ├── __init__.py
│       ├── validators.py         # Валидация URL
│       └── formatters.py         # Форматирование данных
├── .env.example                  # Шаблон переменных окружения
├── .gitignore
├── requirements.txt              # Зависимости
└── README.md                     # Документация
```

## Технологии

- **[aiogram 3.15.0](https://github.com/aiogram/aiogram)** - Telegram Bot API framework
- **[httpx 0.28.1](https://www.python-httpx.org/)** - Асинхронные HTTP-запросы
- **[beautifulsoup4 4.12.3](https://www.crummy.com/software/BeautifulSoup/)** - Парсинг HTML
- **[isodate 0.7.2](https://github.com/gweis/isodate/)** - Парсинг ISO 8601 duration
- **[python-dotenv 1.0.1](https://github.com/theskumar/python-dotenv)** - Управление переменными окружения

## Как это работает

1. **Пользователь** отправляет ссылку на трек
2. **Валидация** проверяет формат URL через regex
3. **HTTP-запрос** получает HTML страницы трека
4. **Парсинг** извлекает JSON-LD метаданные из HTML
5. **Форматирование** преобразует данные (ISO 8601 → MM:SS)
6. **Ответ** отправляется пользователю с информацией о треке

## Обработка ошибок

Бот обрабатывает следующие ситуации:

- ❌ Невалидная ссылка
- ❌ Трек не найден (404)
- ⚠️ Таймаут запроса
- ⚠️ Ошибка сервера Яндекс.Музыки
- ⚠️ Некорректный ввод

Все ошибки логируются для отладки.

## Разработка

### Логирование

Уровень логирования настраивается через переменную `LOG_LEVEL` в `.env`:

- `DEBUG` - Детальная отладочная информация
- `INFO` - Основные события (по умолчанию)
- `WARNING` - Предупреждения
- `ERROR` - Ошибки
- `CRITICAL` - Критические ошибки

### Тестирование

Примеры тестовых ссылок:

**Валидные:**
```
https://music.yandex.ru/album/3192570/track/26464125
https://music.yandex.ru/album/123456/track/789012?from=search
```

**Невалидные:**
```
https://music.yandex.ru/album/3192570
https://music.yandex.ru/artist/12345
https://spotify.com/track/abc123
```

## Деплой

### Локальный запуск

Следуйте инструкциям в разделе "Установка" и "Запуск".

### Хостинг на сервере

Рекомендуемые платформы:
- **Railway** - [railway.app](https://railway.app)
- **Render** - [render.com](https://render.com)
- **VPS** (любой провайдер)

### Docker (опционально)

Создайте `Dockerfile`:

```dockerfile
FROM python:3.10-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY src/ ./src/
COPY .env .

CMD ["python", "src/bot.py"]
```

Запуск:

```bash
docker build -t yandex-music-bot .
docker run --env-file .env yandex-music-bot
```

## Ограничения

- Парсинг зависит от структуры HTML Яндекс.Музыки (может измениться)
- Некоторые треки могут быть недоступны из-за геоблокировки
- Rate limiting со стороны Яндекс.Музыки не документирован

## Лицензия

MIT License

## Поддержка

При возникновении проблем:

1. Убедитесь, что токен бота указан правильно в `.env`
2. Проверьте логи бота для диагностики ошибок
3. Убедитесь, что все зависимости установлены
4. Проверьте, что у вас Python 3.10 или выше

## Автор

Разработано как тестовое задание для демонстрации навыков работы с:
- Telegram Bot API (aiogram)
- Асинхронным программированием (asyncio)
- Web scraping (httpx, BeautifulSoup)
- Парсингом JSON-LD
- Обработкой ошибок и логированием
